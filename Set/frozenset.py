# ⛄️ The frozenset is a function to return a immutable set. (It's like the set method, but immutable.)

mylist = ["apple", "banana", "cherry"]
x = frozenset(mylist)

print(x)  # frozenset({'apple', 'cherry', 'banana'})
